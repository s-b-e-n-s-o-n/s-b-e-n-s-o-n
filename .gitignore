README-simple.md

# macOS launchd config (contains user paths)
com.sbenson.profile-stats.plist

# Local-only checkpoint (machine-specific file paths)
cache/claude_checkpoint.json

# Python
__pycache__/
*.pyc
*.pyo
